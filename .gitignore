bin/
obj/
/packages/
.idea/
.vs/
*.user
riderModule.iml
/_ReSharper.Caches/

# Agent skill payloads installed via `npx skills add` (project-local only)
.agents/skills/
# Symlink farm created by `npx skills add` for Claude Code integration
.claude/skills/
# oh-my-opencode-slim codemap cache
.slim/
# `npx skills add` lockfile — see note below
skills-lock.json
# OpenCode / agent tooling artifacts (regenerable from ~/.config/opencode/skills)
# AGENTS.md mirrors .cursor/rules/*.mdc; opencode.json points OpenCode at those rules
AGENTS.md
opencode.json
# OpenCode local runtime state (queue.json, package metadata, node_modules)
.opencode/
# Per-folder repository maps generated by the codemap skill
codemap.md
# Planning artifacts from /plan-ceo-review and /plan-eng-review
specs/

# OpenCode runtime state (queue, session, undo logs) — regenerable, not part of the repo
.opencode/
